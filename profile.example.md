# 내 프로필 (적합도 계산 입력)

이 파일을 `profile.md`로 복사해 자기 값으로 채운다. `profile.md`는 `.gitignore` 대상이다.
점수 규칙은 [`prompts/fit-spec.md`](./prompts/fit-spec.md) 참고.

> ⚠️ 기술명은 **Notion DB 멀티셀렉트 옵션명과 완전히 같게** 적는다.
> 비교가 완전 일치라서 `리액트`/`react`/`React.js`는 `React`와 매칭되지 않는다.

---

## 연차

```
3
```

경력 연차를 정수로 쓴다. 공고의 `3년+`와 비교된다.

## 보유기술

실무에서 써봤고 면접에서 설명할 수 있는 것만 넣는다.
넉넉하게 적으면 점수가 전부 높게 나와서 정렬이 무의미해진다.

```
React
TypeScript
JavaScript
Next.js
HTML/CSS
Git
REST API
Zustand
React Query
Tailwind
Styled Components
Vite
Webpack
SPA
성능최적화
```

## 미보유기술 (선택)

"학습 중"이거나 "이력서에 못 쓰는" 것. 기록용이며 점수 계산에는 쓰지 않는다.
보유기술에 없으면 자동으로 미보유로 처리되므로, 헷갈리는 항목만 적어두면 된다.

```
React Native
Vue
Angular
Flutter
GraphQL
Node.js
```
